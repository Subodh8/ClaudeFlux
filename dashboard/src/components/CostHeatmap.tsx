'use client';

export function CostHeatmap() {
    // TODO: Render cost heatmap from SSE data
    return (
        <div className="border border-cf-border rounded-lg p-8 bg-cf-surface">
            <p className="text-cf-muted text-center">
                Cost heatmap will display per-agent token and cost breakdown.
            </p>
        </div>
    );
}
